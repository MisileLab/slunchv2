<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
        <!-- windowIsTranslucent removed to fix exit animation issue -->
        <!-- keep both in sync: false for a transparent navigation bar -->
        <item name="android:enforceNavigationBarContrast" tools:targetApi="29">false</item>
        <item name="enforceNavigationBarContrast">false</item>
    </style>

    <style name="SplashViewTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_screen</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
        <item name="android:windowLightStatusBar">false</item> 
    </style>
</resources>
